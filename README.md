# Social Impact

Full-stack starter with:

- `/frontend`: React + Vite app with routing for **Main Dashboard** and **Community Goals**.
- `/backend`: Node.js + Express + MongoDB setup with a `User` schema and a YNAB API route.

## Environment Variables

Use `/backend/.env.example` as your template and provide:

- `YNAB_API_KEY`
